1. **分析问题原因**：每个关键词只有10条结果是因为`multi_keyword_crawler.py`中`search_posts`函数默认`page_size=10`且只爬取第1页

2. **优化爬取逻辑**：

   * 修改`multi_keyword_crawler.py`，增加分页爬取功能，爬取多页结果

   * 调整`page_size`参数，增加每页结果数量

   * 在主函数中添加分页循环，爬取多页数据

3. **优化筛选流程**：

   * 在`summarize_posts.py`中，在总结之前先使用OCR判断帖子是否是大模型相关面试题

   * 为不符合条件的帖子添加删除功能，删除对应的详情文件

   * 优化判断逻辑，提高准确性

4. **测试和验证**：

   * 运行修改后的爬取脚本，验证是否能获取更多结果

   * 运行修改后的总结脚本，验证筛选和删除功能是否正常工作

   * 检查生成的HTML文件，确认只有符合条件的帖子被保留

